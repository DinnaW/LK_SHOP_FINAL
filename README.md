# lkshop-vue componentized

This version splits the original single Vue file into layout, section, product, cart, data, utils, and style files.

## Run

```bash
npm install
npm run dev
```

## Important

Copy your existing image files into `src/assets/` using the names listed in `src/assets/README.md`.
